const int MAX_SIZE = 10;
int intArray(MAX_SIZE);
int currentPos = 0;

int write(int valueToStore)
{
    bool success = true
    if(success)
    {
        return 0;  // succeeded
    }
    else
    {
        return 1;  // failed
    }
}
